# STIG to Markdown Converter Dependencies

# TUI Mode (optional - install with: pip install -r requirements.txt)
textual>=0.40.0        # Modern TUI framework

# Note: CLI mode works without any dependencies (Python standard library only)
# TUI mode requires textual to be installed

# Optional development dependencies:
# pytest>=7.0.0      # For running tests
# black>=23.0.0      # For code formatting
# pylint>=2.17.0     # For linting

